package lexer;
/**
 * Responsible for determining the next state
 */
public class SelectionState implements LexerState {
	public void scan(Character c) {
// TODO: Depending on c switch to next state by calling this.getMyLexer().setState(...)		
	}
}
